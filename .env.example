# Production: set the same name in Vercel → Project → Settings → Environment Variables.
# Must end with /api/v1 (no trailing slash after v1).
VITE_API_BASE_URL=https://digsharing-production.up.railway.app/api/v1
